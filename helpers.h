#ifndef HELPERS_H
#define HELPERS_H
#include "trees.h"
#include <stddef.h>

int exprListSize(const struct ExpressionList* exprList);

char* convertTypeToString(enum TypeNames type);


#endif HELPERS_H
